#include <iostream>

using namespace std;

// A test implementation of serialization to structs in C++
// A deserialized string is passed over, and must be parsed into a struct
int main() {

  std::cout << "Serialization Complete." << std::endl;
  return 0;
}

